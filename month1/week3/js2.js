const printCuteAnimals = (arr1, arr2) => {
  console.log(arr1, `length: ${arr.length}`, arr2, `length: ${arr2.length}`);
  array.pop();
  console.log(arr1, `length: ${arr.length}`, arr2, `length: ${arr2.length}`);
  let newArr = arr.concat(arr2);
  console.log(`The length of the concatenate array: ${newArr.length}`);
  
  return newArrl
};

printCuteAnimals(['river otter', 'ligar', 'Siberian husky'], ['golden bear', 'phenoix']);